// import express from "express";

// import { verifyAdmin } from "../middleware/verifyAdmin.js";
// import { authMiddleware } from "../middleware/authMiddleware.js";
// import { addTheatreOwnerEmail, deleteTheatreOwnerEmail, getTheatreOwnerEmails } from "../controllers/theatreOwnerEmailController.js";

// const router = express.Router();

// router.post("/",authMiddleware, verifyAdmin, addTheatreOwnerEmail);

// router.get("/", authMiddleware, verifyAdmin, getTheatreOwnerEmails);

// router.delete("/:id", authMiddleware, verifyAdmin, deleteTheatreOwnerEmail);

// export default router;
